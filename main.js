var hold = !1; function work() { obsSleep(0).then(() => obsSleep(3)).then(() => obsClick("#ButtonRandom")) } function test() { obsTrue(hold).then(() => obsFunc(work)).then(() => obsSleep(3)).then(() => obsFunc(test)) } function change() { hold = !hold, console.log("now hold is " + hold), document.getElementsByClassName(" monkeyToolBtn btn_check").item(0).textContent = hold ? "stop" : "begin", hold && obsFunc(test) } para = { yAlign: "top", xAlign: "right" }, addToolBtn("btn_check", "begin", change, para);